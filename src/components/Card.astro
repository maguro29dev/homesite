---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class:list={["bg-white rounded-xl border-2 border-brand-accent p-5", className]}>
  <slot />
</div>
